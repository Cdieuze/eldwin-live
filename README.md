# Eldwin Live

A minimal Streamlit app that displays a **single live stress score** (0–100) as a glowing orb with auto-refresh.

## Run locally

```bash
pip install -r requirements.txt
streamlit run app.py
